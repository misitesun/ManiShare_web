# Next.js 全量重构迁移文档

## 1. 文档状态与已确认方向

- 文档版本：v2，2026-09-22。
- 状态：迁移方案已确认，尚未开始修改实现代码。
- 适用范围：Web PC、平板、移动 H5 与 Electron 共用的 React renderer。
- 目标框架：Next.js App Router。迁移完成后，Next.js 是唯一的开发服务器、路由系统和 Web 构建工具。
- 首期部署：`output: 'export'` 静态导出，Electron 继续加载同一份 production 静态产物，不在客户端内启动 Next.js Node 服务。
- 目录决策：`src/app` 专用于 Next App Router；现有 FSD 六层整体移动到 `src/fsd`，避免 Next 将业务 `pages` 层识别为 Pages Router。
- 回滚决策：以迁移前基线提交或独立 worktree 回滚，不在目标分支长期保留 Vite/React Router 双栈。

本文档是实施契约，不代表当前仓库已经完成迁移。实际切换完成时，必须同步修改开发规范、架构文档、模块 README、CI 和自动化契约，使“当前实现”与文档一致。

## 2. “完全使用 Next.js”的完成定义

迁移完成必须同时满足以下条件：

1. `pnpm run dev` 只启动 Next.js，`pnpm run build` 只生成 Next.js production 静态产物。
2. 所有 23 个现有路由契约均由 `src/app` 文件路由承载；动态路径全部通过 `generateStaticParams()` 枚举。
3. 不再存在 `RouterProvider`、`createBrowserRouter`、`react-router-dom`、`src/main.tsx`、根 `index.html` 或 `vite.config.ts`。
4. 不再存在 `import.meta.env`、`VITE_*`、`@vitejs/plugin-react`、`@tailwindcss/vite` 或 Vite 专用类型。
5. `AppLayout` 使用 `children`，不再使用 `Outlet`；导航、参数、查询参数和错误边界全部采用 Next App Router 语义。
6. `src/app` 只放 Next 路由、布局、Metadata 和框架适配，不承载页面业务；FSD 仍遵守 `app → pages → widgets → features → entities → shared`。
7. `dist/staging`、`dist/production`、`dist/electron` 与 `release` 的公共产物契约保持明确，Electron 能直接加载 production 静态导出。
8. Web 与 Electron 均无 hydration、CSP、静态资源 404 或动态深链错误。
9. Vite 与 React Router 依赖、配置、脚本、文档和 lint 规则在同一切换批次中删除，不留下“默认走 Next、部分工具仍走 Vite”的状态。

这里的“完全使用 Next.js”指框架、路由和构建入口全部切换到 Next.js；首期静态导出仍不等同于启用 SSR、ISR 或 Server Actions。

## 3. 目标与非目标

### 3.1 目标

1. 使用 Next App Router 替换 Vite SPA 和 React Router。
2. 为每个可达路由生成独立 HTML，并建立 Metadata、robots、sitemap 与 canonical 的明确边界。
3. 保留现有 React、TypeScript、Tailwind CSS 4、FSD、主题、i18n 基础、动画和共享 UI，不借迁移重做业务页面。
4. 保留同一套语义 DOM 和 mobile-first 响应式实现，继续支持 PC、平板、H5 与 Electron。
5. 让路由参数、静态资源、环境变量、Electron 协议和 CSP 成为可测试的公共契约。
6. 为未来 SSR/ISR 留出升级路径，但不提前引入当前产品不需要的服务端业务。

### 3.2 非目标

- 不在本次迁移中重设页面视觉、改写已确认交互或虚构接口数据。
- 不接入 Server Actions、请求时 Route Handlers、服务端会话、数据库或新认证方案。
- 不把个人资料、笔记、学习进度、AI 对话和收益页面开放给搜索引擎。
- 不全量把 `<img>` 替换为 `next/image`；先完成兼容迁移，再按真实收益单独优化。
- 不启用 PWA、manifest、Service Worker 或离线缓存。
- 不为迁移建立长期 Router 抽象层、兼容重导出或双套页面目录。
- 不在 Electron 内启动常驻 Next.js server。

## 4. 已核实的迁移基线

### 4.1 当前技术基线

| 项目 | 当前状态 |
| --- | --- |
| React | `19.2.8` |
| Vite | `8.2.0` |
| React Router | `7.18.2` |
| TypeScript | `~6.0.2` |
| Tailwind CSS | `4.3.3`，通过 `@tailwindcss/vite` 接入 |
| Electron | `43.4.1` |
| Node.js | `>=22 <25` |
| pnpm | `10.28.2` |
| 路由 | `src/app/config/routes.ts` 中 23 个路径契约 |
| Router 依赖面 | 17 个生产文件、1 个测试文件直接依赖 `react-router-dom` |
| 静态资源 | 216 条图片/SVG 静态导入；其中 97 条位图导入分布在 21 个文件 |
| 图片渲染 | 110 个 `<img>` 使用点 |
| Electron CSP | production 当前使用 `script-src 'self'` |

以上数量是迁移前基线，用于防止漏改，不是永久业务常量。实施前重新运行清单命令；数据或页面已变化时，以当时仓库事实为准。

### 4.2 公共边界影响图

| 边界 | 当前 owner / 消费者 | 目标 owner / 迁移要求 |
| --- | --- | --- |
| Web 入口 | `src/main.tsx`、`src/app/App.tsx` | `src/app/layout.tsx` 与 route layouts；删除手动 `createRoot` |
| 路由 | `src/app/router`、`src/app/config/routes.ts` | `src/app` 文件路由负责装配；`src/fsd/app/config` 只维护业务路径构造器和导航配置 |
| FSD | `src/app`、`src/pages`、`src/widgets`、`src/features`、`src/entities`、`src/shared` | 六层整体移动到 `src/fsd/*`；层级与根 `index.ts` 公共入口不变 |
| App Shell | `src/widgets/app-layout` + `Outlet` | `src/fsd/widgets/app-layout` + `children`；路径状态使用 Next API |
| 全局样式 | `src/app/styles/index.css` | `src/fsd/app/styles/index.css`，只由根 `src/app/layout.tsx` 导入 |
| Tailwind 扫描 | CSS 中 `source("../..")` 扫描 `src` | 移动后更新相对路径，继续只扫描 `src`，同时覆盖 Next route adapters 与 FSD |
| 环境 | `import.meta.env.VITE_*`、Vite mode | 经校验的 `process.env` 构建期/客户端变量与项目环境脚本；Next 的 `NODE_ENV` 不冒充 staging |
| 本地 API | Vite 读取 `API_PROXY_TARGET` 代理 `/api` | 静态导出不提供 rewrites/proxy；按第 10 节采用外部网关或开发直连策略 |
| 静态资源 | Vite import 返回 URL 字符串 | Next 位图 import 返回对象；所有字符串模型和 `<img src>` 消费点显式适配 |
| SEO | 根 `index.html` 单一 Meta | 根/页面 Metadata、`robots.ts`、`sitemap.ts`，公开名单制 |
| Web 构建 | Vite mode 输出 `dist/<mode>` | Next 静态导出后由项目脚本原子整理到既有 `dist` 契约 |
| Electron | SPA fallback 到 `index.html` | 解析 Next 目录路由、`404.html` 与 `/_next/static`；未知文件不得回退为首页资源 |
| CSP | Electron 统一 `script-src 'self'` | 在技术验证门完成静态水合方案，不默认加入宽泛 `unsafe-inline` |
| CI/lint | 多处硬编码 `src/app`、`src/pages` 与 Vite | 同步目录、Next、资源、路由和零残留断言；不得通过忽略绕过 |

### 4.3 已知的硬阻塞

不能采用“根目录 `app/` + 保留 `src/pages/`”的旧方案。Next.js 会分别发现根 `app` 和 `src/pages`，并要求两者位于同一父目录，否则构建直接失败。目标结构必须先消除 `src/pages` 这个框架保留目录名。

## 5. 目标架构

### 5.1 目录结构

```text
src/
├── app/                              # Next App Router，框架适配层
│   ├── layout.tsx                    # html/body、根 Metadata、全局 CSS
│   ├── not-found.tsx
│   ├── global-error.tsx
│   ├── robots.ts
│   ├── sitemap.ts
│   ├── _providers/                   # Next 专用的 Client Provider 桥
│   ├── (shell)/
│   │   ├── layout.tsx                # 装配 AppLayout(children)
│   │   ├── loading.tsx
│   │   ├── error.tsx
│   │   ├── page.tsx                  # /
│   │   ├── ai-tutor/...
│   │   ├── awakening/...
│   │   └── ...
│   └── (standalone)/
│       └── login/page.tsx
└── fsd/
    ├── app/                           # 导航配置、Provider、全局样式等 FSD 应用装配
    ├── pages/                         # 现有业务 page slices
    ├── widgets/
    ├── features/
    ├── entities/
    └── shared/

electron/                              # 安全桌面薄壳
scripts/                               # 环境、构建整理、静态预览、Electron dev
dist/
├── staging/
├── production/
└── electron/
```

约束：

- `src/app` 是 Next 约定目录，也是最外层应用装配；可以依赖 `src/fsd/app`、`pages` 及更低层。
- `src/app/**/page.tsx` 保持薄层，只解析 Next 参数、设置 Metadata、注入路径/数据并组合 page 公共入口。
- FSD 内部继续执行 `app → pages → widgets → features → entities → shared`；除 `app` 和 `shared` 外，同层 slice 不互相依赖。
- 路由适配层不得深层导入业务实现；所需页面组件和静态参数枚举器必须从 slice 根 `index.ts` 导出。
- 删除原 `src/fsd/app/router`；Next 文件路由是唯一 Router owner。
- 使用单一明确别名（建议 `@fsd/* -> src/fsd/*`）或可读相对路径，不建立多套同义 alias。
- 全局样式移动到 `src/fsd/app/styles/index.css` 后，其 Tailwind `source()` 必须继续覆盖整个 `src`，不能遗漏 `src/app` 中的字面量 class。

### 5.2 首期渲染与部署模式

`next.config.ts` 首期采用：

```ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
    output: 'export',
    trailingSlash: true,
}

export default nextConfig
```

静态导出意味着：

- 构建时为每个已知路由生成 HTML，加载后继续由 React 水合并进行客户端导航。
- 可以使用 Server Component 作为构建时路由适配层，也可以使用 Client Component 承载交互。
- 不支持请求时 SSR、ISR、Server Actions、依赖请求的 Middleware/Proxy、rewrites、redirects、headers、默认图片优化或运行时 API 代理。
- 不允许把开发服务器可用误认为静态 production 可用；所有能力必须以导出产物和 Electron 为最终验证对象。
- `next start` 不用于 `output: 'export'`。production-like 验证统一使用 `pnpm run preview` 启动项目自有静态预览脚本。

如果未来确实需要 SSR/ISR，必须新建 ADR，明确 Web Next Server 部署、缓存、认证与成本，并为 Electron 保留独立静态客户端产物；不得直接删除 Electron 离线加载能力。

### 5.3 Server / Client Component 边界

- `layout.tsx`、`page.tsx`、Metadata 和 `generateStaticParams()` 默认保持 Server Component。
- FSD 页面只有在真实使用 Hook、Context、事件处理、Storage、DOM、GSAP、WebGL 或浏览器观察器时才声明 `'use client'`。
- 不把 `'use client'` 放到根 layout，也不为追求“Server Component 数量”拆碎现有稳定业务组件。
- 动态路由由薄 `page.tsx` `await params` 后把已校验字符串作为 props 传给 FSD page，优先移除业务页面中的 `useParams()`。
- 可变查询状态继续由 Client Component 的 `useSearchParams()` 管理，并置于最近的 `Suspense` 边界内，避免静态预渲染失败。
- `usePathname()`、`useRouter()` 只留在实际拥有导航交互的 Client Component；普通链接使用 `next/link`。
- 服务端与客户端首次渲染必须一致。不能在 render 或模块顶层读取视口、Storage、`window`、`document`、Portal target 或随机值。
- 主题默认 dark 的首屏必须确定；持久化主题的 pre-hydration bootstrap 由 `shared/theme` 唯一拥有，并纳入 CSP 技术验证，页面不得复制脚本。

### 5.4 React 工程约束

- 路由级代码分割由 App Router 自然提供，不继续保留 React Router 的 `lazy()` 装配，也不为小组件机械 `dynamic()`。
- State 只保存最小源状态；可推导数据在 render 中推导，不为迁移增加同步 Effect。
- 浏览器监听、计时器、Observer、Object URL、GSAP 与 WebGL 必须保持成对清理。
- 同一语义 DOM 继续覆盖 H5、平板、PC 与 Electron；宽度差异交给 CSS，不在 Client Effect 中维护布局断点。
- `next/link`、`next/navigation` 可以由真实导航 UI owner 使用；不要为掩盖迁移建立永久的伪 Router API。

## 6. 路由全量迁移

### 6.1 路由矩阵

| URL | Next route file | FSD page owner | 壳层 |
| --- | --- | --- | --- |
| `/` | `src/app/(shell)/page.tsx` | `pages/home` | App Shell |
| `/ai-tutor` | `src/app/(shell)/ai-tutor/page.tsx` | `pages/ai-tutor` | App Shell |
| `/ai-tutor/mentors/[mentorId]/scenes/[sceneId]` | `.../ai-tutor/mentors/[mentorId]/scenes/[sceneId]/page.tsx` | `pages/ai-tutor` | App Shell |
| `/awakening` | `src/app/(shell)/awakening/page.tsx` | `pages/awakening` | App Shell |
| `/awakening/courses/[courseId]` | `.../awakening/courses/[courseId]/page.tsx` | `pages/awakening` | App Shell |
| `/login` | `src/app/(standalone)/login/page.tsx` | `pages/login` | 独立 |
| `/profile` | `src/app/(shell)/profile/page.tsx` | `pages/profile` | App Shell |
| `/profile/knowledge-gaps` | `.../profile/knowledge-gaps/page.tsx` | `pages/knowledge-gaps` | App Shell |
| `/profile/notes` | `.../profile/notes/page.tsx` | `pages/notebook` | App Shell |
| `/profile/notes/courses/[coursePackageId]` | `.../profile/notes/courses/[coursePackageId]/page.tsx` | `pages/notebook-course` | App Shell |
| `/profile/notes/courses/[coursePackageId]/chapters/[chapterId]` | `.../chapters/[chapterId]/page.tsx` | `pages/notebook-chapter` | App Shell |
| `/profile/learning-progress` | `.../profile/learning-progress/page.tsx` | `pages/learning-progress` | App Shell |
| `/profile/favorites` | `.../profile/favorites/page.tsx` | `pages/favorites` | App Shell |
| `/profile/works` | `.../profile/works/page.tsx` | `pages/works` | App Shell |
| `/foreign-language-guide` | `.../foreign-language-guide/page.tsx` | `pages/foreign-language-guide` | App Shell |
| `/foreign-language-guide/courses/[courseId]` | `.../foreign-language-guide/courses/[courseId]/page.tsx` | `pages/foreign-language-course` | App Shell |
| `/collaboration` | `src/app/(shell)/collaboration/page.tsx` | `pages/collaboration` | App Shell |
| `/vip` | `src/app/(shell)/vip/page.tsx` | `pages/vip-membership` | App Shell |
| `/leaderboard` | `src/app/(shell)/leaderboard/page.tsx` | `pages/leaderboard` | App Shell |
| `/prize-center` | `src/app/(shell)/prize-center/page.tsx` | `pages/prize-center` | App Shell |
| `/promotion/revenue` | `src/app/(shell)/promotion/revenue/page.tsx` | `pages/promotion-revenue` | App Shell |
| `/branch-company` | `src/app/(shell)/branch-company/page.tsx` | `pages/branch-company` | App Shell |
| `/courses/search` | `src/app/(shell)/courses/search/page.tsx` | `pages/course-search` | App Shell |

Route group 名称不进入 URL。迁移测试必须以 URL 契约为准，不能把目录路径字符串当作用户路径。

### 6.2 路径公共 API

`src/fsd/app/config/routes.ts` 不再保存 React Router 的 `:param` 模板供 `generatePath()` 使用，改为：

- 固定路由导出只读字符串。
- 动态路由导出带明确参数的构造函数。
- 每个路径参数在同一公共能力中执行非空校验和 `encodeURIComponent`。
- 查询参数通过 `URLSearchParams` 构造，不手工拼接未经编码的外部值。
- 页面从 app 注入所需路径或路径构造器，不反向导入 FSD app 层。

动态路径示意：

```ts
export function createAiTutorChatPath(mentorId: string, sceneId: string): string {
    return `/ai-tutor/mentors/${encodePathSegment(mentorId)}/scenes/${encodePathSegment(sceneId)}`
}
```

具体命名以实施时的 app config README 为准；同一条路径只能有一个公共构造入口。

### 6.3 静态参数公共 API

以下动态路由必须实现 `generateStaticParams()`，并把参数来源放在真实数据 owner 的根公共入口：

| 动态路由 | 参数来源 | 当前预期数量 |
| --- | --- | ---: |
| AI 导师聊天 | `pages/ai-tutor` 导出的 mentor/scene 参数枚举器 | 4 × 8 = 32 |
| 言灵觉醒课程 | `pages/awakening` 导出的可达详情参数枚举器 | 2 |
| 外语课程详情 | `pages/foreign-language-guide` 导出的已支持详情参数枚举器 | 1 |
| 课程包笔记 | `entities/notebook` 根入口 | 24 |
| 章节笔记 | `entities/notebook` 按课程包生成章节 | 24 × 8 = 192 |

当前固定路由 18 个，加上 251 个动态实例，预期生成 269 个业务 HTML 路由，另有 404 与 Metadata 文件。测试应从模型计算期望集合，不复制一份 ID 常量；基线数量变化必须能追溯到模型或可达链接变化。

`generateStaticParams()` 返回经过校验的原始领域 ID，由 Next 负责生成 URL；不要把已执行 `encodeURIComponent` 的值传给它。只有导航路径构造函数负责 segment 编码，测试需验证两者指向同一组路由。

每个动态 segment 同时设置并验证未知参数行为。静态导出不能在发布后生成新参数；真实后端开始返回构建时未知 ID 时，必须选择重新构建、改变 URL/客户端数据策略，或通过 ADR 升级 Web 部署模式。

### 6.4 React Router 替换表

| 当前 API | Next 目标 |
| --- | --- |
| `Link` | `next/link` |
| `useNavigate` | `useRouter` |
| `useLocation` | `usePathname` + 必要的 `useSearchParams` |
| `useParams` | Server `page` 的 `params`，必要时 Client `useParams` |
| `useSearchParams` setter | `useSearchParams` 读取 + `useRouter().replace/push` 写入 |
| `generatePath` | 项目路径构造函数 |
| `Outlet` | layout `children` |
| `errorElement` | `error.tsx` / `global-error.tsx` |
| route `Suspense` fallback | `loading.tsx` 或最近的局部 `Suspense` |
| `Navigate` wildcard | `not-found.tsx` 与部署端 404 契约 |

迁移完成后，以源码契约阻断任何新增 `react-router-dom` 导入。

### 6.5 查询参数与未知路由

- 保留现有 `q`、`view=course-packages`、`view=courses`、导师/场景恢复等查询语义。
- 使用 `useSearchParams()` 的静态页必须有局部 `Suspense`，不能通过把整个 root layout 改成 Client Component 规避构建错误。
- 当前未知路径会重定向首页。标准 404 更利于 SEO 与调试，但属于产品行为变化；切换前按第 17 节确认。未确认前，必须在 `404.html`/`not-found.tsx` 中显式保留现有回首页行为，不能依赖旧 SPA fallback 偶然实现。

## 7. Provider、主题与浏览器边界

根 layout 只输出确定性 HTML。Next 专用 Client Provider 桥负责组合现有能力：

- `ThemeProvider`；
- i18n 初始化与现有语言切换基础；
- PWA 安装生命周期预留；
- 未来确有全局作用域的 Client Provider。

迁移检查范围：

- `window`、`document`、Storage、Clipboard、Portal；
- `ResizeObserver`、`IntersectionObserver` 与媒体查询；
- GSAP、View Transition、OGL/WebGL 与 reduced-motion；
- Object URL、事件监听、计时器和异步清理；
- 首屏主题、语言和响应式 DOM 的水合一致性。

规则：

- 模块顶层不得访问仅浏览器存在的 API。
- 主题和语言不能让服务端与客户端第一次 render 产生不同节点树。
- Portal target 在挂载后解析；动画、Observer 和 WebGL 在 Effect/既有封装中启动并清理。
- 不使用 `dynamic(..., { ssr: false })` 作为通用修复。只有确认某个页面私有重型视觉无法预渲染且客户端占位稳定时才局部使用。
- 主题切换继续遵守 `docs/compatibility/theme-transition.md`；Next 迁移不能删除 reduced-motion 与直接切换 fallback。

## 8. 静态资源迁移

Next 的位图静态 import 返回含 `src`、宽高等信息的对象，Vite 返回 URL 字符串。当前 97 条位图 import 中，多处直接写入 `imageSource: string` 或 `<img src={asset}>`，必须显式迁移。

首期策略：

1. 保留业务资源在所属 FSD slice 的 `assets/`，不为省事全部移动到 `public/`。
2. 现有 `<img>` 默认继续使用；位图 import 在消费边界使用 `asset.src`。
3. 模型字段继续保持普通 URL 字符串时，在模型创建处存入 `asset.src`，不让 `StaticImageData` 扩散进领域/UI 公共类型。
4. SVG、CSS 背景、OGL texture 和动态 URL 分别做构建 PoC；不能假设所有扩展名与 Vite 行为相同。
5. 只有固定 URL、Metadata 文件或必须被外部直接访问的资源进入 `public/`。
6. 首期不批量改用 `next/image`。若单独采用，在静态导出下必须配置 `unoptimized` 或已验证的自定义 loader，并复测尺寸、布局和 Electron 协议。
7. 为资源迁移补静态检查或构建断言，确保导出 HTML、CSS 和 JS 不引用 Vite `/assets/*` 旧路径。

资源迁移完成后重新统计 216 条资源导入和 110 个 `<img>` 使用点，逐项说明未迁移项为何与 Next 兼容。

## 9. Tailwind、样式与响应式

- 移除 `@tailwindcss/vite`，使用 Tailwind CSS 4 的 PostCSS 接入。
- `src/fsd/app/styles/index.css` 仍是唯一全局 CSS 入口，只在 `src/app/layout.tsx` 导入一次。
- 移动后修正 `source()` 相对路径，使扫描范围覆盖整个 `src`，但不包含 docs、scripts 和测试 fixture。
- 主题原始颜色仍只存在于全局语义 token；业务组件不新增硬编码色或 `dark:`/`light:` 分支。
- 保留 mobile-first：默认 H5、`md` 平板、`xl` PC/Electron 和既有 ≥1920px 规则。
- 不用服务端 UA 判断布局，不使用根字号、`vw` 或整页缩放，不复制 desktop/mobile DOM。
- Next route adapter 中若出现 Tailwind class，也必须是完整字面量并纳入源码契约扫描。

迁移不改变既有页面的版心、网格和语义字号契约。出现视觉差异时优先修复构建、CSS 顺序或水合问题，不借机修改设计。

## 10. 环境与 API 开发策略

### 10.1 环境变量映射

| 当前变量 | Next 目标 | 说明 |
| --- | --- | --- |
| `VITE_APP_NAME` | `NEXT_PUBLIC_APP_NAME` | 浏览器可见 |
| `VITE_APP_DESCRIPTION` | `APP_DESCRIPTION` | 仅构建时用于 Metadata 默认描述 |
| `VITE_ROBOTS` | `ROBOTS_POLICY` | 仅构建时使用，默认保持 noindex |
| `VITE_API_BASE_URL` | `NEXT_PUBLIC_API_BASE_URL` | 浏览器请求基址 |
| `VITE_API_TIMEOUT` | `NEXT_PUBLIC_API_TIMEOUT` | 正整数校验 |
| `VITE_DEPLOY_ENV` | `NEXT_PUBLIC_DEPLOY_ENV` | `development`、`staging`、`production` |
| `VITE_DEFAULT_LANGUAGE` | `NEXT_PUBLIC_DEFAULT_LANGUAGE` | 当前支持值校验 |
| 无 | `SITE_URL` | 允许索引时必填的构建期绝对 HTTPS canonical 基址 |
| `API_PROXY_TARGET` | 不直接映射到客户端 | 见下方开发代理策略 |

规则：

- `NEXT_PUBLIC_*` 会编译进浏览器产物，禁止密钥、令牌、内网凭据或 Electron 私有配置。
- Next 的 `NODE_ENV` 只使用框架允许的 `development`、`test`、`production`；staging 是业务部署环境，不设置 `NODE_ENV=staging`。
- 项目环境脚本负责按确定顺序加载 `.env` 与目标环境文件、校验公开变量，然后启动 Next。优先使用 Node 22 能力，不新增 `dotenv` 运行时依赖。
- `NEXT_PUBLIC_DEPLOY_ENV` 必须与脚本目标一致；production/staging 构建不能复用已污染的父进程环境。
- 允许索引前，`SITE_URL` 必须存在并通过 URL/HTTPS 校验；否则构建保持 noindex，不能生成伪 canonical。

### 10.2 `/api` 与开发代理

`output: 'export'` 不支持 Next rewrites/proxy。不能只在 `next dev` 中配置代理并让 production/Electron 静默失效。

首期按以下顺序处理：

1. 当前页面没有真实 API 时，保留 `shared/api` 的 `/api` 默认契约，但不宣称 Next 已提供代理。
2. 接入真实 Web API 前，优先由部署层和本地开发网关统一代理 `/api`；这样静态站点与 production 行为一致。
3. 如果本地直接访问后端，则在忽略的 development 环境文件中填写公开绝对 URL，并由后端明确配置 CORS。
4. Electron 接入真实 API 时使用经过确认的 HTTPS 绝对基址，不能把 `englishlearning://app/api` 当作后端。
5. `API_PROXY_TARGET` 在最终清理时删除；只有项目确实实现并测试了外部开发网关 runner 时才保留，并同步环境文档。

## 11. 构建、预览与产物契约

### 11.1 最终命令语义

```text
pnpm run dev                 # Next development 环境
pnpm run dev:staging         # Next dev + staging 公开配置，仅用于联调
pnpm run typecheck           # Next/TS 类型检查
pnpm run build               # lint + production Next 静态构建
pnpm run build:staging       # staging Next 静态构建
pnpm run build:production    # production Next 静态构建
pnpm run preview             # 静态服务 dist/production，不调用 next start
pnpm run desktop:dev         # Next dev + Electron
pnpm run desktop:build       # production renderer + Electron main/preload
pnpm run desktop:package     # 打包安装程序
```

不新增指向 `next start` 的 `start` 脚本。若外部平台强制要求 `start`，它只能明确等价于静态 preview，并在文档中说明，不得伪装成 Next server 部署。

### 11.2 产物整理

```text
dist/staging/       # 预发 Web 静态产物
dist/production/    # 生产 Web + Electron renderer 静态产物
dist/electron/      # Electron main/preload
release/            # Electron 安装包，不提交
```

项目脚本必须：

1. 在独立临时位置完成 Next 构建与静态导出。
2. 校验入口 HTML、所有路由 HTML、`404.html` 和 `/_next/static` 存在。
3. 校验导出环境与目标目录一致。
4. 通过同文件系统 rename 或等价策略原子替换 `dist/<environment>`，失败时不留下“部分新、部分旧”产物。
5. 不把 `.next` 缓存、开发文件或 Next server 文件复制进 Electron 包。

`pnpm run preview` 使用项目脚本提供 trailing-slash 目录解析和 404 行为，不能用一个只会回退 `index.html` 的 SPA server 掩盖导出错误。

## 12. Electron 迁移

### 12.1 production 协议解析

`englishlearning://` 继续作为标准、安全自定义协议。解析规则至少覆盖：

```text
englishlearning://app/                  -> dist/production/index.html
englishlearning://app/vip/             -> dist/production/vip/index.html
englishlearning://app/profile/notes/   -> dist/production/profile/notes/index.html
englishlearning://app/_next/static/... -> dist/production/_next/static/...
```

必须保留：

- URL 解码、NUL/非法编码处理和目录穿越阻断；
- `contextIsolation: true`、`sandbox: true`、`nodeIntegration: false`；
- 具名 preload API、IPC sender 校验、拒绝新窗口与非可信导航；
- 正确 MIME type、状态码和不可变静态资源缓存策略；
- 文件存在时只返回该文件，目录路径显式查找 `index.html`；
- 未知页面返回 `404.html`，未知 JS/CSS/图片必须返回 404，禁止回退到 HTML。

当前 `existsSync(directory)` 后直接 fetch 目录、任意缺失资源回退 `index.html` 的 SPA 行为不能保留，否则 Next chunk 404 会被伪装为 HTML 并导致白屏。

### 12.2 CSP 技术验证门

这是批量迁移前的阻断项。Next 静态 HTML 的水合脚本必须在 Electron 当前安全边界下实际运行。

按以下优先级验证：

1. 使用 production 静态导出和当前严格 `script-src 'self'` 做最小水合 PoC。
2. 若需要严格 hash/SRI，验证 Next 当前版本的 webpack-only experimental SRI 是否覆盖本项目导出形态，并固定 bundler 与构建断言。
3. 若仍有确定的内联脚本，为每个导出 HTML 生成可复现的 CSP hash manifest，由协议层按文档返回对应策略，并测试篡改/漏 hash 失败路径。
4. nonce 需要每请求动态渲染，不适用于本项目静态导出，不能作为首期方案。
5. 不默认加入 `'unsafe-inline'`。若安全评审明确接受临时放宽，必须单独 ADR、限定 production/dev 差异并记录关闭条件。

主题 bootstrap、Next 水合、页面切换、GSAP/OGL 和动态 import 必须一起验证。PoC 未通过时停止批量路由迁移，先修正架构选择。

### 12.3 development

`desktop:dev` 调整为：

1. 构建或 watch Electron main/preload。
2. 在 `127.0.0.1` 启动 Next dev server。
3. 由脚本选择/确认端口并做 HTTP readiness 探测，不依赖脆弱的控制台文案解析。
4. 通过 `DESKTOP_RENDERER_URL` 启动 Electron。
5. 任一子进程结束时清理其他进程，SIGINT/SIGTERM 均可退出。

开发 CSP 与 production CSP 分开验证；开发工具需要的 `unsafe-eval` 或 WebSocket 不能泄漏到 production 协议策略。

## 13. SEO 与 Metadata

### 13.1 默认安全策略

- development、staging 和正式公开前一律 `noindex,nofollow,noarchive`。
- 只有无需登录、无个人数据、内容长期稳定且产品明确批准的页面才进入可索引名单。
- 私有页面使用页面级 `noindex,nofollow`，不能只依赖根 robots 文件。
- 页面主要文本必须出现在静态 HTML，不能仅在挂载后由客户端生成。
- JSON-LD 只使用真实已确认数据，不虚构价格、评分、库存或课程属性。

### 13.2 待产品确认的候选矩阵

| 路由 | 初始策略 |
| --- | --- |
| `/foreign-language-guide` | 确认为公开导览后可索引 |
| `/foreign-language-guide/courses/*` | 确认为公开详情后可索引 |
| `/collaboration` | 确认为公开介绍页后可索引 |
| `/vip` | 确认为公开方案页后可索引 |
| `/awakening/**` | 待确认是否允许未登录访问 |
| `/login` | `noindex` |
| `/` | 若为个人仪表盘则 `noindex` |
| `/profile/**` | `noindex` |
| `/ai-tutor/**` | `noindex` |
| `/promotion/**`、`/branch-company` | `noindex` |
| `/courses/search` | `noindex` |

公开页交付包括唯一 title/description、canonical、必要的 Open Graph 信息、robots 与 sitemap 收录。未确认正式域名时保持 noindex，不用占位域名生成 canonical。

## 14. 分阶段实施计划与退出条件

### 阶段 0：冻结基线与技术验证门

实施：

1. 在独立迁移分支/worktree 记录基线提交，运行 test、lint、production/staging build 与 Electron build。
2. 生成 23 条路由、动态 ID、查询参数、17+1 Router 依赖文件和资源导入清单。
3. 在实施当天确认稳定版 Next 与 React 19、TypeScript 6、Node 22、Tailwind 4、Electron 43 的版本矩阵，禁止 canary。
4. 做最小 PoC：`src/app` + `src/fsd` 目录发现、Tailwind PostCSS、一个动态导出路由、一个位图 import、`useSearchParams` Suspense、Electron 自定义协议、水合和 CSP。
5. 记录 Next 静态导出/CSP/目录决策 ADR。

退出条件：PoC 在浏览器和 Electron production 静态产物中成功水合，无 CSP/资源错误；否则不得进入批量迁移。

### 阶段 1：FSD 目录机械迁移

实施：

1. 将六层整体移动到 `src/fsd/*`，先不改业务行为。
2. 更新相对导入、tsconfig、测试 runner、资源脚本、源码契约、模块文档检查和所有受影响文档路径。
3. 把全局样式移动到 `src/fsd/app/styles/index.css`，同步 Tailwind source 路径。
4. 删除空目录和过时公共入口，不增加兼容重导出。

退出条件：仍以迁移前 renderer 跑通基线命令，确认本阶段只有目录/契约变化。Vite 基线只保留到下一阶段原子切换，不与 Next 同时作为默认入口。

### 阶段 2：Next 全量路由与运行时切换

实施：

1. 安装稳定版 `next` 与 Tailwind PostCSS 依赖，更新 lockfile 和安装脚本白名单审查。
2. 建立 `src/app` 根 layout、route groups、错误/加载边界和全部 23 个路由 adapter。
3. `AppLayout` 改为 `children`；迁移 17 个生产 Router 消费者与 1 个测试。
4. 建立固定路径、动态路径构造函数和静态参数公共 API。
5. 划分最小 Client Component；迁移 Provider、主题、i18n、PWA 预留和浏览器 API。
6. 适配全部位图/SVG/资源消费点。
7. 新增 Next 环境、typecheck、build、产物整理与 preview 脚本。
8. 把 `dev`、`build`、`build:staging`、`build:production` 切换到 Next。

退出条件：Web 端 269 个当前业务路由可构建、直接访问、刷新和客户端导航；源码中 Router/Vite API 为零。

### 阶段 3：Electron 切换

实施：

1. 改造协议解析、目录 index、404、MIME、缓存和缺失资源行为。
2. 落地阶段 0 选定的 CSP 方案及自动化验证。
3. 重写 Next + Electron 开发进程编排。
4. 更新 Electron Builder 文件范围，只包含 production 静态产物与 main/preload。

退出条件：production 构建能无网络启动；固定页、动态页、查询参数、刷新、返回和 `_next/static` 均正常，无 CSP/hydration/IPC 安全回归。

### 阶段 4：Metadata、SEO 与部署契约

实施：

1. 根和页面 Metadata、robots、sitemap、canonical。
2. staging/production 环境加载和 `SITE_URL` 校验。
3. 部署端 trailing-slash、404 和缓存规则。
4. 验证公开 HTML 主文本与私有页 noindex。

退出条件：未确认公开名单时全站保持 noindex；确认后只有名单内 URL 进入 sitemap 和 indexable Metadata。

### 阶段 5：清理、同步与最终审查

实施：

1. 删除 Vite、React Router、旧入口、旧配置、Vite 类型和旧环境变量。
2. 从 `pnpm-workspace.yaml` 移除只为 Vite/`esbuild` 保留且已无消费者的安装脚本白名单。
3. 同步 AGENTS、开发/TS/测试/架构/协作文档、环境、能力地图、Electron README、模块 README、兼容性索引、ADR 和 CI。
4. 更新源码契约及 fixture 测试，阻断旧目录和旧框架重新进入。
5. 执行完整验证和只读 change review，修复 findings 后交付。

退出条件：满足第 2 节全部完成定义和第 16 节 Definition of Done。

## 15. 测试与验证计划

### 15.1 自动化

```bash
pnpm run format:check
pnpm run test
pnpm run lint
pnpm run typecheck
pnpm run build
pnpm run build:staging
pnpm run build:production
pnpm run desktop:build
```

需要新增或更新的自动断言：

- 23 个路由契约与 Next route manifest 一致。
- 当前所有可达动态 ID 都以原始值出现在 `generateStaticParams()` 结果中，不重复、不为空，并与编码后的导航路径一一对应。
- production 当前预期生成 269 个业务 HTML；断言由模型集合推导，而非复制 ID。
- `dist/<environment>` 含入口、动态页、404、Metadata 与 `_next/static`，且不含 `.next` server/cache 文件。
- 路径构造函数覆盖特殊字符、空值、查询参数和返回路径。
- 环境 loader 拒绝 mode 不匹配、非法 timeout、可索引但缺失 site URL 等配置。
- Electron resolver 覆盖目录、文件、404、编码、穿越、缺失 chunk 和正确 Content-Type。
- CSP 覆盖正常水合、缺失/篡改 hash 失败和 production 禁止开发放宽项。
- 源码中不存在 `react-router-dom`、`import.meta.env`、`VITE_*`、Vite 入口或未授权 Node/Electron renderer import。
- 静态资源测试覆盖位图对象到 URL 的转换以及导出路径存在性。

### 15.2 Web PC 人工验收

未指定设计尺寸时至少使用 1440×900；涉及版心的页面增加 1280、1500、1919、1920 和 2560px：

- 逐页对比迁移前视觉基线。
- 直接访问、客户端跳转、刷新、前进/返回、查询参数恢复正常。
- dark/light 首屏、切换、持久化和 reduced-motion 正常。
- 侧栏、抽屉、页签、Tooltip、Dialog、Select、分页、Portal 与滚动正常。
- GSAP、OGL/WebGL 无重复初始化、资源泄漏或水合闪烁。
- 控制台无 hydration、CSP、资源 404、重复 key 或 Effect 清理错误。
- Network 中没有 chunk 请求返回 HTML，没有意外 API 或逐条补全请求。

### 15.3 Electron 人工验收

- development 与 production 两种模式都能启动。
- 首页、二级页和动态深链可直接打开、刷新和返回。
- `_next/static`、页面私有图片、字体、主题和动画正常。
- 离线启动 production；关闭/重开窗口正常。
- 非可信导航、新窗口和伪造 IPC 被拒绝。
- macOS 与 Windows 正式签名、notarization、安装包行为仍由发布任务验收。

### 15.4 用户验收

- 768×1024 平板视觉、横竖屏和真机操作。
- 390×844 H5 视觉、安全区、输入、滚动和真机操作。
- 正式部署域名、CDN/静态服务器规则和真实后端连通性。

Codex 不得把未执行的平板、H5、真机或正式发布验收记录为已通过。

## 16. Definition of Done

- [ ] Next App Router 是唯一 renderer/router/build 入口。
- [ ] 23 个路由契约全部迁移，所有动态可达参数全部静态生成。
- [ ] `src/app` 与 `src/fsd` 所有权清晰，FSD lint 和公共入口规则通过。
- [ ] 17 个生产 Router 消费文件与 1 个测试完成迁移。
- [ ] 97 条位图 import、其他 SVG/资源和 110 个 `<img>` 使用点已检查并适配。
- [ ] Vite、React Router、旧入口、旧配置、旧类型、旧环境变量和无用依赖全部删除。
- [ ] `pnpm run dev`、staging/production build、preview 与 Electron 使用同一 Next 路由契约。
- [ ] `dist/staging`、`dist/production` 和 `dist/electron` 可重复生成且互不污染。
- [ ] Electron 深链、目录路由、404、chunk、CSP、水合、IPC 与离线启动通过。
- [ ] Metadata/robots/sitemap 遵守公开名单，默认不泄露私有页面。
- [ ] test、lint、typecheck、全部 Web build 和 desktop build 通过。
- [ ] PC 视觉与交互完成；平板/H5 明确交由用户验收。
- [ ] 文档、README、ADR、CI、环境模板、lint 与测试和最终实现同步。

## 17. 实施前仍需确认的产品/部署项

这些事项不改变 Next 全量迁移方向，但会影响最终发布配置：

1. 允许搜索引擎索引的最终路由名单。
2. 未知路由使用标准 404，还是继续自动回首页。
3. Web 正式域名、静态托管/CDN、canonical 基址和 `/api` 网关方式。
4. Electron 正式品牌名、app ID、图标、签名、notarization 和自动更新方案。

技术实施默认值：在以上事项确认前全站 noindex；不伪造 canonical；保留现有未知路径行为；不接入真实 API 或正式桌面发布配置。

## 18. 回滚与协作策略

- 迁移使用独立分支或 worktree，迁移前保留通过验证的 Vite 基线提交。
- 阶段 1 可以让 Vite 继续作为唯一基线入口；阶段 2 切换后，目标分支只允许 Next 作为默认入口。
- 不用两个 Router 同时接管页面，不在业务组件中加入 `if (vite)` / `if (next)` 分支。
- 默认命令、CI、产物整理和 Electron Builder 在同一切换批次中更新。
- 若切换失败，整体回到基线提交或 worktree；不手工删除一半配置来“临时恢复”。
- 同一工作区的目录搬迁、路由改造和文档同步只允许一个写入 Agent；review 保持只读。

## 19. 主要风险与缓解

| 风险 | 影响 | 强制缓解 |
| --- | --- | --- |
| `src/pages` 被 Next 识别 | 构建直接失败或意外启用 Pages Router | 六层整体移动到 `src/fsd`，lint 阻断新的 `src/pages` |
| 静态导入类型变化 | TypeScript 错误、图片失效 | 97 条位图清单、模型边界 `.src` 转换、导出资源断言 |
| 动态路由漏生成 | 深链/刷新 404 | owner 公共枚举器、路由集合测试、产物 HTML 清单 |
| `useSearchParams` 破坏静态构建 | build 失败或整页 CSR | 最近 `Suspense`、查询状态专项构建测试 |
| 水合不一致 | 闪烁、交互失效 | 确定性首屏、最小 Client 边界、浏览器 API 延后 |
| Electron 严格 CSP 阻断 Next | 桌面白屏 | 阶段 0 production PoC，SRI/hash 方案先行，不默认 unsafe-inline |
| 缺失 chunk 回退 HTML | MIME 错误、随机白屏 | 资源严格 404，只有目录路由解析 index.html |
| staging/production 混用 | 错误 API/SEO/品牌配置 | 独立环境 loader、匹配校验、原子产物目录 |
| 开发代理只在 Next dev 可用 | production/Electron 请求失败 | 外部同源网关或明确绝对 API URL，不使用静态 export 不支持的 rewrite |
| 迁移时顺带重构业务 | 范围和回归失控 | route adapter 保持薄层，视觉/业务变更另立任务 |
| 静态导出被误当完整 Next server | 引入不可部署能力 | 文档、lint、CI 明确禁止首期不支持特性 |
| 私有页被索引 | 数据与隐私风险 | 默认 noindex、公开名单、页面级 Metadata 与产物检查 |

## 20. 文档与契约同步清单

实际迁移必须同步：

- `AGENTS.md`
- `docs/development-standards.md`
- `docs/typescript-standards.md`
- `docs/testing-standards.md`
- `docs/architecture.md`
- `docs/ai-collaboration.md`
- `docs/environment.md`
- `docs/module-capabilities.md`
- `docs/compatibility.md` 与新增 Electron/Next CSP 契约
- FSD app、路由配置、全局样式、共享配置和 Electron README
- Next 迁移 ADR
- `scripts/validate-source-contracts.mjs` 及其 fixture 测试
- `scripts/validate-module-docs.mjs`
- `.github/workflows/ci.yml`
- `.env*` 模板、`.gitignore`、`electron-builder.yml`

在代码真正切换前，上述文档仍描述当前 Vite 实现；切换批次中再把“迁移目标”改成“当前契约”，不得提前宣称已经完成。

## 21. 官方参考

- [Next.js App Router 项目结构](https://nextjs.org/docs/app/getting-started/project-structure)
- [从 Vite 迁移到 Next.js](https://nextjs.org/docs/pages/guides/migrating/from-vite)
- [Next.js 静态导出](https://nextjs.org/docs/app/guides/static-exports)
- [Next.js SPA 指南](https://nextjs.org/docs/app/guides/single-page-applications)
- [generateStaticParams](https://nextjs.org/docs/app/api-reference/functions/generate-static-params)
- [useSearchParams](https://nextjs.org/docs/app/api-reference/functions/use-search-params)
- [Next.js 环境变量](https://nextjs.org/docs/app/guides/environment-variables)
- [Next.js Content Security Policy](https://nextjs.org/docs/app/guides/content-security-policy)
- [Next.js Metadata 与 OG 图片](https://nextjs.org/docs/app/getting-started/metadata-and-og-images)
- [Next 16.3.5 路由目录发现源码](https://github.com/vercel/next.js/blob/v16.3.5/packages/next/src/lib/find-pages-dir.ts)
