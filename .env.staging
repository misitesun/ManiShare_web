VITE_DEPLOY_ENV=staging
VITE_DEFAULT_LANGUAGE=zh-CN

# Staging defaults to the same-origin gateway. Override only when the staging
# deployment uses a dedicated public API origin.
VITE_API_BASE_URL=/api
