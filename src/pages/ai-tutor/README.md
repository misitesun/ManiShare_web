# AI 助教

- 职责：Figma `779:9855` 的导师和场景选择静态页面；入口 `index.ts` 导出 `AiTutorPage`。
- 约束：所有目录数据和设计资源归本页所有，不调用接口、不补全数据、不申请麦克风权限。开启对话仅展示按钮，不创建会话或跳转。
- 交互：导师默认 Sarah；场景默认未选。两组原生单选复用 TldSelection，状态仅保留在当前页面；CTA 复用 TldButton large 规格。
- 布局：手机一列、md 两列、xl 四列；图片按 291:200 保持比例。1920px 起沿用应用 250px 内容边距，内容自然滚动，兼容黑白主题。
- 资源：assets 的 12 张图片来自本次 Figma 设计稿，不使用远端运行时图片请求。导师名称与口音文案按设计稿保留。
- 扩展：真实对话、声音试听、会话权限及 API 接入需另行确认，不根据当前静态稿推断业务。
- 验证：test/lint/build；本次用户指定最终效果直接人工验收，PC、平板和 H5 视觉效果均待用户验收。
